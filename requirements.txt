# Parking Portfolio Intelligence — dependencies
# Python 3.12 required (MetricFlow has no Python 3.13 wheels yet)
#
# Install:
#   python3.12 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#   cd dbt_project && dbt deps

# ── dbt core + DuckDB adapter ─────────────────────────────────────────────
dbt-core==1.8.9
dbt-duckdb==1.8.4

# ── MetricFlow semantic layer (brings in metricflow + dbt-metricflow) ─────
# Installs the `mf` CLI for running semantic layer queries locally
dbt-metricflow[duckdb]

# ── DuckDB engine ─────────────────────────────────────────────────────────
duckdb==1.1.3

# ── Streamlit app ─────────────────────────────────────────────────────────
streamlit==1.35.0
plotly==5.22.0
pandas==2.2.2
protobuf>=3.20,<5        # streamlit 1.35 requires protobuf <5

# ── Claude API (NL query tab) ─────────────────────────────────────────────
anthropic>=0.28.0

# ── Data generation ───────────────────────────────────────────────────────
faker==25.0.0

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1
